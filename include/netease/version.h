/* libnetease: C port of go-musicfox/netease-music (v1.6.0) + its CLI shell.
 *
 * Layout mirrors the Go two-layer design:
 *   src/vendor/  — zero-dependency crypto primitives (AES/MD5/RSA/base64/hex)
 *   src/core/    — request kernel, cookie jar, JSON map (Go-semantics)
 *   src/service/ — API endpoints (one family per file)
 *   src/cli/     — netease-cli shell, stdout protocol 1:1 with the Go build
 */
#ifndef NETEASE_H
#define NETEASE_H
#endif
